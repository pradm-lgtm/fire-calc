{
  "_comment": [
    "CSS/text selectors for the Sleeper web UI, kept out of the code because",
    "they are the part that breaks when the site is redesigned. Fix them here",
    "and the submitter keeps working.",
    "Capture real values with:  python3 submitter.py --inspect LEAGUE_ID",
    "which opens the page so you can copy selectors from devtools."
  ],
  "sleeper": {
    "league_url": "https://sleeper.com/leagues/{league_id}/team",
    "players_url": "https://sleeper.com/leagues/{league_id}/players",
    "search_box": "input[placeholder*='Find player' i]",
    "player_row": "text={player_name}",
    "claim_button": "button:has-text('Claim'), button:has-text('Add')",
    "drop_select": "select[name*='drop' i]",
    "drop_option": "option:has-text('{drop_name}')",
    "bid_input": "input[type='number'], input[name*='bid' i]",
    "confirm_button": "button:has-text('Confirm'), button:has-text('Submit')",
    "success_marker": "text=/claim (placed|submitted)/i",
    "_comment": [
      "Sleeper renders no <button> elements - its controls are clickable divs -",
      "so text-based selectors are used rather than tag or role ones.",
      "Confirm each against the live page with:",
      "  python3 submitter.py USER --probe LEAGUE_ID --player 'Some Player'"
    ]
  }
}
