{
  "_comment": [
    "CSS/text selectors for the Sleeper web UI, kept out of the code because",
    "they are the part that breaks when the site is redesigned. Fix them here",
    "and the submitter keeps working.",
    "Capture real values with:  python3 submitter.py --inspect LEAGUE_ID",
    "which opens the page so you can copy selectors from devtools."
  ],
  "sleeper": {
    "league_url": "https://sleeper.com/leagues/{league_id}/team",
    "players_url": "https://sleeper.com/leagues/{league_id}/players",
    "search_box": "input[placeholder*='Find player' i]",
    "player_row": "div.scrolled-name:text-is('{abbrev}')",
    "claim_button": "a.player-action-button.waiver",
    "drop_select": "",
    "drop_option": "div.player-name-with-position:has(span.name-text:text-is('{drop_name}'))",
    "bid_input": "input[type='number'][placeholder*='Bid' i]",
    "confirm_button": "button:has-text('MAKE WAIVER BID')",
    "success_marker": "",
    "_comment": [
      "All confirmed against the live site.",
      "Sleeper uses almost no semantic HTML: controls are clickable divs,",
      "there is no <select>, and the ONLY <button> on the page is the final",
      "MAKE WAIVER BID.",
      "Name forms differ by context: the players table abbreviates",
      "(T. Ferguson) while the claim dialog's roster list does not",
      "(Woody Marks).",
      "Re-check after a redesign with:",
      "  python3 submitter.py USER --probe LEAGUE_ID --player 'Full Name'"
    ]
  }
}
